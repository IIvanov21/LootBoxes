workers:
  hosts:
    swarm-worker:
  vars:
    ansible_user: jenkins
    ansible_ssh_private_key_file: '~/.ssh/id_rsa'

manager:
  hosts:
    swarm-manager:
  vars:
    ansible_user: jenkins
    ansible_ssh_private_key_file: '~/.ssh/id_rsa'

loadbalancer:
  hosts:
    load-balancer:
  vars:
    ansible_user: jenkins
    ansible_ssh_private_key_file: '~/.ssh/id_rsa'